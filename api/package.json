{
  "name": "api",
  "author": "Francois Wouts <f@zenc.io>",
  "private": true,
  "scripts": {
    "generate": "yarn generate-client && yarn generate-server",
    "generate-client": "apidef generate typescript rentals.api ../client/src -c https://rentals.toptal.zenc.io",
    "generate-server": "apidef generate typescript rentals.api ../server/src -s"
  },
  "devDependencies": {
    "@zenclabs/api": "^0.0.11"
  }
}
